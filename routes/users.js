// router
// { auth }
// { getCurrentUser, modifyUserData }
// { validateUser }

/* router.use(auth);
router.get("/me", getCurrentUser);
router.patch("/me", validateUser, modifyUserData);

module.exports = router; */
