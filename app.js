// dotenv
// express
// mongoose
// cors

// app
// port = 3001
// errors

// app.use(cors())
// app.options("*")
// { requestLogger, errorLogger }
// { validateUserBody, validateAuthenication }

// mongoose all

// signin
// signup

// app.use((error, req, res, next))
